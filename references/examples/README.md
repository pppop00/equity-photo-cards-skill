# Archived examples

The JSON examples in this directory predate schema v5 and are preserved as historical assets only. They include CFA-era fields and cannot be passed to the active renderer. Use `references/templates/card_slots.template.json` and rerun content production for a current five-card bundle.
